# Parameters

Source of truth: `../../RetainingWallSubassembly_Plan.md`.

Phase 1 only creates the documentation placeholder. The next implementation phase should add the `Side`, `WallCase`, override, PCC, filter, and wall-top offset parameter details from the plan.
